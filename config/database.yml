# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: mysql2
  database: techradar_devel
  host: localhost
  user: root

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  database: techradar_test
  host: localhost
  user: root

production:
  adapter: mysql2
  database: techradar_production
  host: localhost
  user: root
